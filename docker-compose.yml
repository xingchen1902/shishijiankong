version: "3.8"

services:
  ark-monitor:
    build: .
    container_name: ark-monitor
    restart: always
    ports:
      - "8899:8899"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Shanghai

networks:
  default:
    name: ark-monitor-net
